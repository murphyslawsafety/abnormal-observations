# Cloudflare Worker + static assets.
# Dashboard project name: cj (Workers & Pages → cj)
#
# Build settings for the EXISTING cj project:
#   Build command:                         (leave empty)
#   Deploy command:                        npx wrangler deploy
#   Non-production branch deploy command:  npx wrangler versions upload
#   Path / Root directory:                 /

name = "cj"
main = "src/index.js"
compatibility_date = "2024-08-01"

[assets]
directory = "."
binding = "ASSETS"
run_worker_first = ["/api/*"]

# Shared observer counter (no separate KV setup required).
[[durable_objects.bindings]]
name = "OBSERVER_COUNTER"
class_name = "ObserverCounter"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["ObserverCounter"]

[vars]
# Public observer total will never display below this number.
# Raise this to restore lost joins from the broken-counter period,
# then redeploy. New joins continue counting up from the floor.
STARTING_OBSERVER_COUNT = "1"

# Optional: set OBSERVER_ADMIN_SECRET in Cloudflare dashboard to allow
# PUT /api/observers with Authorization: Bearer <secret> and {"count":N}
